import type { ModuleBodyProps } from '../body-props';

/** The Scripts module; built out in the scenes editor task. */
export function ScriptsBody(_props: ModuleBodyProps): React.JSX.Element {
  return <div />;
}
