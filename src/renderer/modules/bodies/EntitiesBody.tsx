import type { ModuleBodyProps } from '../body-props';

/** The NPCs & objects module; built out in the editor task. */
export function EntitiesBody(_props: ModuleBodyProps): React.JSX.Element {
  return <div />;
}
